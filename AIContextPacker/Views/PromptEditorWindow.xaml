<Window x:Class="AIContextPacker.Views.PromptEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Prompt Editor" Height="400" Width="600"
        Background="{DynamicResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <DockPanel Margin="20">
        <StackPanel DockPanel.Dock="Bottom" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Save" 
                    Click="Save_Click"
                    MinWidth="100"
                    Padding="15,8"
                    Margin="0,0,10,0"/>
            <Button Content="Cancel" 
                    Click="Cancel_Click"
                    MinWidth="100"
                    Padding="15,8"/>
        </StackPanel>

        <StackPanel>
            <TextBlock Text="Prompt Name:" 
                       Margin="0,0,0,5"
                       Foreground="{DynamicResource TextBrush}"/>
            <TextBox x:Name="NameTextBox" 
                     Padding="8"
                     Margin="0,0,0,20"/>

            <TextBlock Text="Prompt Content:" 
                       Margin="0,0,0,5"
                       Foreground="{DynamicResource TextBrush}"/>
            <TextBox x:Name="ContentTextBox" 
                     Height="200"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Padding="8"/>
        </StackPanel>
    </DockPanel>
</Window>
